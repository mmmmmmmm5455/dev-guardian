{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Agent 3 — Skill Repair & Evolution Agent Input",
  "description": "Input schema for the Skill Repair & Evolution Agent (skill-repair-agent)",
  "type": "object",
  "properties": {
    "repair_id": { "type": "string" },
    "execution_output_path": {
      "type": "string",
      "description": "Agent 2 的輸出 JSON 檔案路徑"
    },
    "auto_fix_enabled": {
      "type": "boolean",
      "description": "是否自動修復可修復的問題",
      "default": true
    },
    "max_auto_fix_attempts": {
      "type": "integer",
      "description": "每個問題的最大自動修復嘗試次數",
      "default": 3,
      "minimum": 1,
      "maximum": 5
    }
  },
  "required": ["repair_id", "execution_output_path"]
}
